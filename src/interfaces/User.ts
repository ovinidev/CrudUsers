export interface UsersData {
  name: string;
  age: string;
  email: string;
  role: string;
  _id: string;
}

export interface UserProps {
  name: string;
  age: string;
  email: string;
  role: string;
}
